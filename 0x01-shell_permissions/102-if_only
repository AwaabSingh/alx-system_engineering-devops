#!/bin/bash

# Check if file exists and is owned by guillaume
if [[ -f "hello" && $(stat -c "%U" hello) == "guillaume" ]]
then
  # Change owner to betty
  chown betty hello
  echo "File owner changed to betty"
else
  echo "File hello does not exist or is not owned by guillaume"
fi

